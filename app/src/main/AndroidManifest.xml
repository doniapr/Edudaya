<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.don.edudaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".musicProvActivity" />
        <activity android:name=".musik" />
        <activity android:name=".adatProvActivity" />
        <activity android:name=".AdatActivity" />
        <activity android:name=".KuisActivity" />
        <activity android:name=".HasilActivity" />
        <activity android:name=".TentangActivity"></activity>
    </application>

</manifest>